{
  "name": "map-viz-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node scripts/prepare-data.mjs && node scripts/prepare-permits.mjs && node scripts/prepare-buildings.mjs",
    "build:buildings": "node scripts/prepare-buildings.mjs",
    "geocode:permits": "node scripts/geocode-permits.mjs",
    "translate:permits": "node scripts/translate-permits.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "fast-xml-parser": "^5.5.10",
    "proj4": "^2.20.6",
    "shapefile": "^0.6.6",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
